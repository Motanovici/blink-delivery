import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'blink-delivery',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
